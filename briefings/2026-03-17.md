---
# HX Trading Briefing — 16 Mar 2026

## GP was soft over the last 7 days vs the same week last year: we made £143k, down £27k or about 16%, with the biggest hit from direct single-trip and partner referral single sales while market demand stayed strong.

---

## At a Glance

- 🔴 **Direct single hurt most** — Over the last 7 days vs the same week last year, direct single-trip GP fell £12k to £31k, mostly because fewer web visits turned into searches and we made 22% less on each sale.
- 🔴 **Europe value leak** — Over the last 7 days vs the same week last year, Europe GP fell £20k to £92k, with policy volume down only 3%, so the bigger problem was making about 15% less per sale.
- 🔴 **Partner single referrals softened** — Over the last 7 days vs the same week last year, partner referral single-trip GP fell £6k to £14k because sales volume dropped 27% and higher prices were eaten up by higher commission and underwriter cost.
- 🔴 **Bronze is dragging** — Over the last 7 days vs the same week last year, Bronze GP fell £9k to £28k, mostly in direct Europe single-trip journeys where lower-value sales have been building for weeks.
- 🟡 **Direct annual volume missed demand** — Over the last 7 days vs the same week last year, direct annual GP fell £8k to £41k because we sold 16% fewer policies, which means less future renewal income coming through.

---

## What's Driving This

### Direct Single GP decline `RECURRING`

Over the last 7 days vs the same week last year, direct single-trip GP fell £12k to £31k. Traffic and conversion both hurt: mobile sessions were down 15%, desktop sessions were up 11% but fewer visitors got through to search, and we then made 22% less per policy.  
The data shows a real funnel problem, not a weak market. This has been negative on 9 of the last 10 days, with the biggest leak in mobile search entry and lower-value Bronze and Silver mix.

```sql-dig
SELECT booking_source,
  SUM(policy_count) AS policies,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS gp,
  SUM(CAST(total_gross_inc_ipt AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_price,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_gp
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND distribution_channel = 'Direct'
  AND policy_type = 'Single'
GROUP BY booking_source
ORDER BY gp DESC;
```

### Partner Referral Single volume loss `RECURRING`

Over the last 7 days vs the same week last year, partner referral single-trip GP fell £6k to £14k. We cannot see partner traffic, but volume was down 27% and average GP per sale was basically flat, so this was mainly fewer referred sales rather than a conversion issue.  
The higher selling price did not help because commission and underwriter cost took more out of each booking. This has been negative on 8 of the last 10 days, so it looks like a real partner channel problem.

```sql-dig
SELECT booking_source,
  SUM(policy_count) AS policies,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS gp,
  SUM(CAST(total_paid_commission_value AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_commission
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND distribution_channel = 'Partner Referral'
  AND policy_type = 'Single'
GROUP BY booking_source;
```

### Europe destination GP decline `EMERGING`

Over the last 7 days vs the same week last year, Europe GP fell £20k to £92k even though policy volume was only down 3%. That means the main issue was value per sale, not demand disappearing.  
The weakness is concentrated in direct single-trip and lower-tier products, especially Bronze. Traffic appears to be there in the market, but we are capturing too much cheap Europe demand.

```sql-dig
SELECT distribution_channel, policy_type,
  SUM(policy_count) AS policies,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS gp,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_gp
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND destination_group = 'Europe'
GROUP BY distribution_channel, policy_type
ORDER BY gp DESC;
```

### Overall Bronze cover-level deterioration `EMERGING`

Over the last 7 days vs the same week last year, Bronze GP fell £9k to £28k, with policies down 6% and GP per policy down 18%. Traffic into these journeys was weaker, especially on mobile, and fewer visitors got far enough to search.  
This is not a Bronze issue everywhere. It is mainly a direct Europe acquisition problem, and it lines up with the direct single decline above.

```sql-dig
SELECT distribution_channel, policy_type,
  SUM(policy_count) AS policies,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS gp,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_gp
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND cover_level_name = 'Bronze'
GROUP BY distribution_channel, policy_type
ORDER BY gp DESC;
```

### Direct Annual GP decline `EMERGING`

Over the last 7 days vs the same week last year, direct annual GP fell £8k to £41k because we sold 16% fewer policies. That matters because annual growth is how we invest in future renewal income.  
Traffic into search looks like the main cause again, especially in Bronze and Silver annual web journeys. This is a volume miss, not an economics problem.

```sql-dig
SELECT cover_level_name,
  SUM(policy_count) AS policies,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS gp,
  SUM(CAST(total_gross_inc_ipt AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_price,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_gp
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND distribution_channel = 'Direct'
  AND policy_type = 'Annual'
GROUP BY cover_level_name;
```

### Partner Referral Annual decline `NEW`

Over the last 7 days vs the same week last year, partner annual GP fell £4k to £8k, with volume down 19%. That is a miss on annual acquisition, so it means less future renewal income later on.  
This may be concentrated in older, higher-value and cruise-leaning partner accounts rather than a broad channel collapse. The base is smaller here, so treat it as early evidence rather than proven.

```sql-dig
SELECT insurance_group,
  SUM(policy_count) AS policies,
  SUM(CAST(total_paid_commission_value AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_commission,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS gp
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND distribution_channel = 'Partner Referral'
  AND policy_type = 'Annual'
GROUP BY insurance_group
ORDER BY gp DESC;
```

### Aggregator Single GP decline `NEW`

Over the last 7 days vs the same week last year, aggregator single-trip GP was down only about £500 to £2k, but that flatters the picture because volume jumped 49% while average GP per sale nearly halved from about £3 to £2.  
We cannot see aggregator traffic, but the data suggests heavier price pressure on comparison sites. This may be early evidence that we are buying cheap sales with poor value.

```sql-dig
SELECT insurance_group,
  SUM(policy_count) AS policies,
  SUM(CAST(total_gross_inc_ipt AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_price,
  SUM(CAST(total_paid_commission_value AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_commission,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS gp
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND distribution_channel = 'Aggregator'
  AND policy_type = 'Single'
GROUP BY insurance_group
ORDER BY gp;
```

### Renewals GP growth `NEW`

Over the last 7 days vs the same week last year, renewal GP rose £3k to £52k as renewed policies increased 11% even though fewer annual policies were expiring. That points to better retention, which is good news.  
The signal is encouraging, but the base is smaller and timing can move renewals around, so treat this as promising rather than fully proven.

```sql-dig
SELECT policy_renewal_year,
  SUM(policy_count) AS renewed_policies,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64)) AS renewed_gp,
  SUM(CAST(total_gross_exc_ipt_ntu_comm AS FLOAT64))/NULLIF(SUM(policy_count),0) AS avg_gp
FROM `hx-data-production.commercial_finance.insurance_policies_new`
WHERE transaction_date BETWEEN '2026-03-09' AND '2026-03-16'
  AND distribution_channel = 'Renewals'
  AND policy_type = 'Annual'
GROUP BY policy_renewal_year
ORDER BY policy_renewal_year;
```

---

## Customer Search Intent

According to Google Sheets Insurance Intent and Dashboard Metrics data, travel insurance search demand is up about 65% to 74% over the last 4 weeks vs the same period last year, so the market is giving us more demand than we are converting. According to AI Insights, “travel insurance comparison” is up about 375% and “do I need travel insurance” is up about 31%, which points to customers shopping around harder and wanting clearer value messaging. According to AI Insights, Europe sun destinations like Spain, the Canaries, Algarve and Greece remain the strongest areas of interest, which fits with Europe being our biggest value leak rather than a demand problem. According to the Dashboard Metrics tab, 4-week momentum is still up about 38%, so demand is building rather than fading.  
**Source:** Google Sheets — Insurance Intent tab  
**Source:** Google Sheets — Dashboard Metrics tab  
**Source:** AI Insights — what_matters, divergence, quarterly

---

## News & Market Context

According to [Jet2’s Summer 2026 programme](https://www.jet2.com/news/2024/09/Jet2_com_and_Jet2holidays_launch_biggest_ever_Summer_programme_for_2026?utm_source=openai), Jet2 is adding more capacity to Spain, the Canaries and the Algarve, which supports the Europe-heavy demand showing up in search data. According to AI Insights, easyJet and TUI are also adding Mediterranean capacity, which usually lifts late-booked and heavily compared travel insurance demand. According to [ITIJ](https://www.itij.com/latest/news/millions-uk-ehicghic-cards-set-expire-2025-raising-insurance-concerns?utm_source=openai), millions of EHIC and GHIC cards are due to expire, which is likely adding to insurance and GHIC-related searches. According to AI Insights, that lines up with the rise in “do I need travel insurance” searches. According to [Yahoo News](https://uk.news.yahoo.com/british-airways-issues-today-flights-130432343.html?utm_source=openai) and [The Week](https://theweek.com/personal-finance/how-travel-insurance-works-if-your-holiday-is-disrupted-by-war?utm_source=openai), BA disruption and war-related exclusions are keeping disruption cover in the news, so clear wording in customer messaging matters. According to [Saga](https://www.saga.co.uk/travel-insurance/middle-east-travel-disruption?utm_source=openai), competitors are leaning into reassurance on disruption cover, which is a message worth watching.

---

## Actions

| Priority | What to do | Why (from the data) | Worth |
|----------|-----------|---------------------|-------|
| 1 | Pull pricing, UX and marketing into a same-day fix session on direct single mobile journeys, starting with landing-to-search on Bronze and Silver Europe pages | Direct single lost £12k/week and the clearest leak is mobile traffic and search entry | ~£12k/week |
| 2 | Review Europe direct single pricing and value message on Bronze first, then Silver | Europe GP is down £20k/week with volume only down 3%, so we are monetising badly, not just missing demand | ~£20k/week |
| 3 | Shift paid search into high-intent comparison, GHIC/EHIC and Europe destination terms and send traffic straight into faster quote starts | Market demand is up sharply YoY, but direct single and direct annual are missing that demand | ~£8k/week |
| 4 | Rework conversations with the weakest partner referral accounts on both single and annual, starting with those showing the biggest volume drop | Partner referral single and annual are down about £10k/week combined, mainly on volume | ~£10k/week |
| 5 | Tighten aggregator single pricing guardrails on the cheapest comparison-site sales | Aggregator single volume is up, but value per sale has nearly halved | ~£1k/week |

---

_Generated 07:22 16 Mar 2026 | 23 investigation tracks | gpt-5_

---
*Generated 06:12 17 Mar 2026 | Tracks: 23 + Follow-ups: 33 | Model: gpt-5.4*
